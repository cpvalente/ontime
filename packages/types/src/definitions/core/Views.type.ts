export type ViewSettings = {
  overrideStyles: boolean;
  endMessage: string;
  normalColor: string;
  warningColor: string;
  warningThreshold: number;
  dangerColor: string;
  dangerThreshold: number;
};
