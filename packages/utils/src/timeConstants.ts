/**
 * Milliseconds in a second
 */
export const mts = 1000;

/**
 * Milliseconds in a minutes - 1000 * 60
 */
export const mtm = 60000;

/**
 * Milliseconds in a hours - 1000 * 60 * 60
 */
export const mth = 3600000;

/**
 * Milliseconds in a day - 1000 * 60 * 60 * 24
 */
export const dayInMs = 86400000;
